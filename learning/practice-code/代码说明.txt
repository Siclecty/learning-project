Test01:
定义一个Test01类;
在类中定义主方法，在主方法中编写输出语句;

Test02:
定义一个Test02类,编写主方法;
在输出语句中分别输出各种信息;

Test03:
定义类Test03，编写主方法;
定义变量(其中数据类型有:八种基本数据类型包括byte、short、int、long、float、double、char、boolean);
在输出语句中，直接调用变量名;

Test04:
定义类Test04，编写主方法;
在主方法中定义不同的变量;
使用输出语句，输出变量;

Test05:
定义类Test05，编写主方法;
在主方法中，定义两个int类型的变量;
定义第三个变量，将前两个变量相加，赋值给第三个变量;
使用输出语句，输出变量;

Test06:
定义类Test06，编写主方法;
在主方法中定义不同的变量;
将已知字符串与变量使用“+”拼接在一起输出;

Reward：
定义类Reward;
使用Scanner完成键盘录入；
采用if...else...if格式；
为每种判断设置对应的条件；
为每种判断设置对应的奖励；

CryptographicCheck：
定义类CryptographicCheck;
使用Scanner录入用户输入的密码，并使用变量接受;
采用if...else格式；

 Computer
定义类Computer;
使用Scanner录入三个变量;
使用switch判断语句对第三个变量进行判断，匹配要执行的操作;
在每一个case中分别对第一个变量和第二个变量进行不同的操作;

Square:
定义类Square;
定义循环执行4次代表一共4行;
定义内层循环执行5次代表每行5个符号;
在内层循环中输出符号;
每行结束之后执行换行操作;

